@extends('admin.layouts.app')

@section('title', 'Séries d\'événements')

@section('content')

<div class="max-w-8xl mx-auto py-8">
    <div class="flex justify-between items-center mb-8 border-b pb-2">
        <h1 class="text-3xl font-bold text-dark gradient-text">Séries d'Événements ({{ $series->total() }})</h1>
        <a href="{{ route('admin.event-series.create') }}"
            class="py-2 px-4 rounded-lg text-white font-semibold bg-green-600 hover:bg-green-700 transition duration-300 shadow-md flex items-center">
            <i class="fas fa-plus-circle mr-2"></i> Nouvelle Série
        </a>
    </div>

    @if (session('success'))
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <strong class="font-bold">Succès!</strong>
            <span class="block sm:inline">{!! session('success') !!}</span>
        </div>
    @endif
    @if (session('error'))
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <strong class="font-bold">Erreur!</strong>
            <span class="block sm:inline">{!! session('error') !!}</span>
        </div>
    @endif

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        @forelse ($series as $serie)
            <div class="bg-white rounded-xl shadow-2xl overflow-hidden border border-gray-100 transform hover:scale-[1.02] transition duration-300 relative">
                
                <a href="{{ route('admin.event-series.show', $serie) }}" class="block h-40 overflow-hidden group">
                    @php
                        $imageUrl = $serie->getFirstMediaUrl('main_image');
                        $placeholder = 'https://placehold.co/800x400/4c1d95/ffffff?text=' . urlencode($serie->name_fr);
                    @endphp
                    <img src="{{ $imageUrl ?: $placeholder }}" alt="{{ $serie->name_fr }}"
                        class="w-full h-full object-cover transition duration-500 group-hover:opacity-90 group-hover:scale-105"
                        onerror="this.onerror=null;this.src='{{ $placeholder }}';">
                </a>

                <div class="p-5">
                    <div class="flex justify-between items-start mb-2">
                        <span class="text-xs font-semibold px-3 py-1 rounded-full 
                            @if($serie->is_active) bg-green-100 text-green-800 @else bg-red-100 text-red-800 @endif">
                            {{ $serie->is_active ? 'Active' : 'Inactive' }}
                        </span>
                        @if($serie->is_featured)
                            <span class="text-xs font-semibold px-3 py-1 rounded-full bg-red-100 text-red-800">
                                Vedette
                            </span>
                        @endif
                    </div>

                    <h2 class="text-xl font-bold text-gray-900 mb-2 truncate" title="{{ $serie->name_fr }}">
                        {{ $serie->name_fr }}
                    </h2>

                    <div class="text-sm text-gray-600 space-y-1 mt-3 border-t pt-3">
                        @if($serie->venue_name)
                            <p class="flex items-center"><i class="fas fa-map-marker-alt w-5 text-primary mr-2"></i>
                                {{ $serie->venue_name }}, {{ $serie->city }}
                            </p>
                        @endif
                        @if($serie->start_date)
                            <p class="flex items-center"><i class="fas fa-calendar-alt w-5 text-primary mr-2"></i>
                                {{ \Carbon\Carbon::parse($serie->start_date)->format('d M Y') }}
                                @if($serie->end_date)
                                    - {{ \Carbon\Carbon::parse($serie->end_date)->format('d M Y') }}
                                @endif
                            </p>
                        @endif
                        <p class="flex items-center"><i class="fas fa-ticket-alt w-5 text-primary mr-2"></i>
                            {{ $serie->events_count }} match(es)
                        </p>
                    </div>

                <div class="p-5 pt-0 flex justify-between items-center border-t mt-3">
                    <a href="{{ route('admin.event-series.show', $serie) }}"
                        class="text-sm font-semibold text-primary hover:text-purple-700 transition duration-150 flex items-center">
                        <i class="fas fa-eye mr-2"></i> Voir Détails
                    </a>
                    <div class="flex space-x-2">
                        <a href="{{ route('admin.event-series.edit', $serie) }}" title="Modifier"
                            class="text-blue-600 hover:text-blue-800 p-2 rounded-full hover:bg-blue-100 transition duration-150">
                            <i class="fas fa-edit"></i>
                        </a>
                        <form action="{{ route('admin.event-series.destroy', $serie) }}" method="POST"
                            onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette série ?');">
                            @csrf
                            @method('DELETE')
                            <button type="submit" title="Supprimer"
                                class="text-red-600 hover:text-red-800 p-2 rounded-full hover:bg-red-100 transition duration-150">
                                <i class="fas fa-trash"></i>
                            </button>
                        </form>
                    </div>
            </div>
        @empty
            <div class="col-span-full bg-white p-6 rounded-xl shadow-lg border border-gray-100 text-center">
                <p class="text-xl text-gray-500">Aucune série d'événements trouvée.</p>
                <a href="{{ route('admin.event-series.create') }}" class="text-primary hover:underline mt-2 inline-block">
                    Créer la première série
                </a>
            </div>
        @endforelse
    </div>

    <div class="mt-8">
        {{ $series->links() }}
    </div>

@endsection
